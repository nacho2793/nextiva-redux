import React from 'react';
import './Header.scss';

const header = () => {
  return (
    <header className="header">
        <span className="logo">nextiva</span>
    </header>
 )
}

export default header;